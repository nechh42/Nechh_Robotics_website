# NECHH ROBOTICS — requirements.txt
requests==2.31.0
supabase==2.3.0
python-telegram-bot==20.7
apscheduler==3.10.4
fastapi==0.109.0
uvicorn==0.27.0
ollama==0.1.7

# ════════════════════════════════════════════════════════
# KURULUM SIRASI
# ════════════════════════════════════════════════════════
#
# 1. pip install -r requirements.txt
#
# 2. Supabase:
#    → https://supabase.com → yeni proje
#    → SQL Editor → subscription.py içindeki SQL bloğunu çalıştır
#    → URL ve ANON KEY'i kopyala
#
# 3. NOWPayments:
#    → https://nowpayments.io → ücretsiz hesap
#    → API key al → subscription.py içine yaz
#    → IPN URL: https://SENIN_URL/webhook/nowpayments
#
# 4. Ollama:
#    → https://ollama.com → kur
#    → ollama pull deepseek-r1:7b
#
# 5. CryptoPanic:
#    → https://cryptopanic.com/developers/api/ → token al
#    → sentiment.py içine yaz
#
# 6. Telegram:
#    → @BotFather → /newbot → token al
#    → bot_manager.py ve webhook_server.py içine yaz
#    → Kanalları oluştur → kanal ID'lerini subscription.py PLANS'a ekle
#
# ════════════════════════════════════════════════════════
# ÇALIŞTIRMA
# ════════════════════════════════════════════════════════
#
# Terminal 1 — Abonelik botu:
#   python bot_manager.py
#
# Terminal 2 — Webhook sunucu:
#   uvicorn webhook_server:app --host 0.0.0.0 --port 8000
#
# Terminal 3 — Scheduler (health + AI analiz):
#   python scheduler.py
#
# Mevcut botun ayrı çalışmaya devam eder — HİÇBİR DEĞİŞİKLİK GEREKMİYOR.
#
# ════════════════════════════════════════════════════════
# DOSYA HİYERARŞİSİ
# ════════════════════════════════════════════════════════
#
# nechh/
# ├── formatter.py       → Mesaj şablonları
# ├── sentiment.py       → Fear&Greed + haber verisi
# ├── ai_analyst.py      → Ollama AI analiz
# ├── subscription.py    → NOWPayments + Supabase
# ├── bot_manager.py     → Telegram bot (abonelik)
# ├── scheduler.py       → Otomatik gönderimler
# ├── webhook_server.py  → NOWPayments IPN
# └── requirements.txt
#
# Mevcut botun → AYRI KLASÖR, dokunulmuyor.
